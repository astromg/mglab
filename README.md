# mglab
personal lab with jupyter etc. 


### prepare .venv to work with this repo
```
python3 -m venv .venv
source .venv/bin/activate
```

and then work localy:
```
pip install tqdm
pip install numpy
pip install scipy
pip install matplotlib
pip install astropy

```

